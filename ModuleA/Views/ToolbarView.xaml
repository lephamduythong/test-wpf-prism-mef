<UserControl x:Class="ModuleA.Views.ToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModuleA.Views"
             mc:Ignorable="d" 
             >
    <StackPanel Margin="5">
        <Button Margin="0,5" Command="{Binding ActiveContentViewCommand}">Activate Content View</Button>
        <Button Margin="0,5" Command="{Binding DeactivateContentViewCommand}">Deactivate Content View</Button>
        
    </StackPanel>
</UserControl>
